#pragma once

enum class UserRole { Default, RESEARCHER, ADMIN_STAFF, SYSTEM_ADMIN };

enum class ReservationStatus { Default, PENDING, APPROVED, REJECTED, CANCELLED };

enum class Weekday { Default, Monday, Tuesday, Wednesday, Thursday, Friday };

enum class TimeSlot { Default, Morning, Afternoon, Evening };